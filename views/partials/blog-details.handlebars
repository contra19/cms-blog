<div class="card">
  <div class="card-header">
    <h2>{{title}}</h2>
  </div>
  <div class="card-body">
    <p>{{body}}</p>
  </div>
  <div class="card-footer text-muted">
    <small>Created by {{User.username}} on {{format_date date}}</small>
  </div>
</div>
