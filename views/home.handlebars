<div class="container mt-4">
    <div class="row">
        <div class="col-12 mb-4">
            <h2 class="recent-posts-title">Recent Blog Posts</h2>
        </div>
    </div>
    <div class="row">
        {{#each blogs}}
            {{> blog-card blog=this isHome=true}}
        {{/each}}
    </div>
</div>
